// let table = { "html": "text/html", "png": "image/png", "gif": "image/gif" }
// let fnames = ["animated.gif", "portrait.png", "index.html"]
// fnames.forEach((fname) => {
//   let a_fname = fname.split('.')
//   let a_length= a_fname.length - 1
//   let extension = (fname.includes('.') && a_fname[a_length] != '.' && a_fname[a_length] != undefined) ? a_fname[a_length].toLowerCase() : ""
//   // # For each of the Q filenames, display on a line the corresponding MIME type. If there is no corresponding type, then display UNKNOWN.
//   console.log(extension in table ? table[extension] : "UNKNOWN")
//   // console.log(extension)
// })
